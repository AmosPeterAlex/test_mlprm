import 'package:flutter/material.dart';

class LapHome extends StatelessWidget {
  const LapHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      //set a frame to shell route all buttons
      body: Center(child: Text("lap home")),
    );
  }
}
